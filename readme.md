# Customer Sentiment Checker

Система автоматического анализа тональности клиентских отзывов с использованием больших языковых моделей (LLM).

## 📋 Описание проекта

**Customer Sentiment Checker** — это программное решение для автоматизации анализа клиентских отзывов. Проект использует возможности больших языковых моделей для предоставления бизнесу глубоких, практических инсайтов о мнениях, настроениях и проблемах клиентов.

### Проблема, которую решает проект

Современные бизнесы собирают огромные объемы клиентских отзывов из различных источников:
- Отзывы о продуктах
- Комментарии в социальных сетях  
- Тикеты службы поддержки
- Опросы и формы обратной связи

Ручная обработка этих данных неэффективна, дорога и не масштабируется. В результате ценные инсайты, которые могли бы улучшить продукт, повысить удовлетворенность клиентов и снизить отток, часто теряются.

## 🏗️ Архитектура проекта
Согласно ТЗ, проект состоит из двух сервисов: API для классификации отзывов в реальном времени и дашборда для анализа загруженных отзывов

**API состоит из двух основных компонентов:**

### 📁 `backend/`
Эндпойнты

### 📁 `processor/`
Обработка с помощью llm, схемы для входного и выходного форматов

**Дашборд состоит из следующих компонентов:**

### 📁 `backend/`
Серверная часть приложения, содержащая бизнес-логику и API.

### 📁 `database/`
База данных, содержащая исходные отзывы, а также результаты из обработки.

### 📁 `frontend/`
Пользовательский интерфейс для взаимодействия с системой анализа тональности.

### 📁 `senana/`
Серверная часть для выполнения анализа сентимента

#### 🚀 Ключевые возможности

- **Анализ тональности**: Классификация текста как позитивного, негативного или нейтрального
- **Аспектный анализ**: Определение конкретных аспектов продукта/услуги и связанной с ними тональности
- **Извлечение ключевых слов и тем**: Автоматическое выделение ключевых тем из больших объемов отзывов
- **REST API**: Программный доступ к движку анализа
- **Масштабируемость**: Архитектура рассчитана на обработку растущих объемов данных

## 📂 Структура репозитория

```
📁 Структура проекта 

    api/ — сервис для обработки отзывов через REST API.   
    ├──    main.py — точка входа (FastAPI).  
    ├──    processor/ — логика разбора отзывов и формирования аннотаций.  
    ├──    schemas/ — Pydantic-модели для валидации запросов и ответов.  
    ├──    utils/ — вспомогательные функции (работа с JSON, тайминги).  
    └──    ya_cloud_llm/ — интеграция с Yandex Cloud LLM для классификации и сентимент-анализа.
         

    backend/ — бизнес-логика и эндпойнты для дашборда.   
    ├── main.py — основной скрипт.  
    ├── api/v1/              # API endpoints (версия 1)
    ├── core/                # Config, mappings, exceptions
    ├── models/              # SQLAlchemy модели
    ├── repositories/        # Data access layer
    ├── schemas/             # Pydantic схемы
    └── services/            # Business logic
         

    frontend/ — веб-интерфейс дашборда (TS / React). 

    database/ — файлы баз данных . 

    dataset/ — данные и аналитические ноутбуки использованные для подготовки данных:   
    ├──    classify/ — эксперименты с классификацией (GPT, Yandex, постобработка).  
    ├──    clustering/ — кластеризация отзывов и выделение тем.  
    └──    metrics/ — скрипты для расчёта F1-micro и Accuracy.
         

    local_classificator/ — локальный классификатор на основе LLM для заполнения базы. 

    senana/ — аналитика дашборда.  
     
```

## 🛠️ Технологический стек

- **Backend**: Python, FastAPI
- **Frontend**: TS / React
- **AI/ML**: LangChain, Ollama, LLM модели
- **Конфигурация**: YAML


## 📖 Документация

### 📡 API (`/api`)

**Эндпоинты:**

- `GET /health` — проверка доступности сервиса.  
  **Ответ:** `{"status": "ok"}`

- `POST /analyze` — основной эндпоинт для анализа отзывов.  
  **Вход:**
  ```json
  {
    "data": [
      {"id": 1, "text": "Отзыв клиента..."},
      {"id": 2, "text": "Еще один отзыв..."}
    ]
  }
    ```

 **Выход:**
  ```json
{
  "predictions": [
    {"id": 1, "topics": ["Карты", "Приложение / сайт"], "sentiments": ["позитив", "негатив"]},
    {"id": 2, "topics": ["Обслуживание в офисе"], "sentiments": ["нейтрально"]}
  ]
}
   ```
**Как работает:**
-    Запрос валидируется схемой AnalyzeRequest.
-    Каждый отзыв обрабатывается параллельно (до 4 потоков) через Yandex Cloud LLM.
-    Модели передаётся системный промпт с инструкцией на русском языке.
-    Результаты собираются, сортируются по id и форматируются в требуемый JSON.
-    Обработка 250 отзывов укладывается в ≤ 3 минут.
-    При ошибках возвращается структурированный ответ или HTTP 500.



### 🗃️ Структура базы данных

**Таблицы:**

- **`reviews`**  
  - `review_id` (INTEGER, PRIMARY KEY)  
  - `date` (DATE)  
  - `text` (TEXT)  
  - `source_id` (INTEGER, внешний ключ → `sources.id`)

- **`annotations`**  
  - `review_id` (INTEGER, FK → `reviews.review_id`)  
  - `category_id` (INTEGER, FK → `categories.id`)  
  - `sentiment_id` (INTEGER, FK → `sentiments.id`)

- **`sources`**  
  - `id` (INTEGER, PK)  
  - `name` (TEXT) — значения: `"Banki.ru"`, `"Sravni.ru"`, `"yandex"` и др.

- **`categories`**  
  - `id` (INTEGER, PK)  
  - `name` (TEXT) — например: `"Карты"`, `"Кредиты"`, `"Приложение / сайт"` и т.д.

- **`sentiments`**  
  - `id` (INTEGER, PK)  
  - `name` (TEXT) — значения: `"позитив"`, `"негатив"`, `"нейтральный"`

> ⚠️ **Важно**: первичный ключ в `reviews` — это **`review_id`**, а не `id`.

---
### 📊 Backend Dashboard

**Назначение**:  
Сервис для агрегации и отдачи аналитических данных по отзывам: распределение по категориям, тональностям и динамика во времени.

---

#### 🗃️ Структура кода

- **`app/api/v1/`** — эндпоинты REST API (версия 1)  
- **`app/core/`** — конфигурация, маппинги (БД ↔ API), кастомные исключения  
- **`app/models/`** — SQLAlchemy-модели, отражающие структуру `reviews.db`  
- **`app/repositories/`** — слой доступа к данным (запросы к БД)  
- **`app/schemas/`** — Pydantic-схемы для валидации и сериализации ответов  
- **`app/services/`** — бизнес-логика: агрегация, фильтрация и т.п.
- **`database/`** — файл SQLite (`reviews.db`) с обработанными отзывами и аннотациями
---
### 🖥️ Frontend (`/frontend`)

**Назначение**:  
Интерактивный дашборд **Customer Voice Analytics** для визуализации тональности отзывов, распределения по категориям и динамики во времени.

---

#### ⚙️ Технологический стек
- **React 18** + TypeScript  
- **Vite** — сборка и dev-сервер  
- **React Query** — управление состоянием API  
- **Tailwind CSS** + **shadcn/ui** — стили и компоненты  
- **Recharts** — графики  
- **React Router v6** — навигация

---

#### 🗃️ Структура кода
- **`components/`** — UI-компоненты: фильтры, карточки, графики, лейаут  
- **`pages/`** — страницы: дашборд, анализ продуктов, список отзывов  
- **`contexts/`** — глобальное состояние (фильтры, конфигурация)  
- **`hooks/`** — кастомные хуки для данных и фильтров  
- **`lib/`** — API-клиент и сервисы (`dashboardApi`, `reviewsApi`, `configApi`)  
- **`types/`** — TypeScript-типы для API и внутренних данных

---

#### 🔌 Интеграция с бэкендом
- Все запросы идут на `VITE_API_BASE_URL` (указывается в `.env`)  
- Используются эндпоинты из **Backend Dashboard** (`/categories`, `/sentiment-distribution`, `/trends`, `/reviews`)  
- Данные кэшируются через React Query, обновляются при смене фильтров  
- Поддержка фильтрации по:  
  - дате (`dateRange`)  
  - источнику (`source`)  
  - категории (`category`)  
  - тональности (`sentiment`)

---


### 🧠 ML Pipeline

**Назначение**:  
Подготовка тематических категорий, классификация отзывов и расчёт метрик качества для финальной оценки по ТЗ.



#### 📁 Структура кода
- **`dataset/clustering/`** — ноутбуки для выявления тем из сырых отзывов.  
- **`dataset/classify/`** — эксперименты с классификацией (GPT, Yandex LLM) и постобработка результатов.  
- **`dataset/metrics/`** — скрипты для расчёта `F1-micro` (70%) и `Accuracy` (30%).  
- **`local_classificator/`** — локальный LLM-классификатор для наполнения базы данных.

---

#### 🔁 Последовательность обработки

1. **Кластеризация → формирование категорий**  
   - Отзывы разделялись на атомарные аспекты с помощью LLM.  
   - Применялась zero-shot классификация: LLM «придумывала» ~300 тем.  
   - Темы кластеризовались (BERTopic), объединялись в обобщённые группы.  
   - На основе этого создан адаптивный классификатор: использовал фиксированный список + опцию добавления новых тем.  
   - Финальный список из **11 категорий** доработан вручную и утверждён как эталон.

2. **Классификация → наполнение БД**  
   - Использовался LLM с жёстко заданным списком из 11 категорий.  
   - Постобработка:  
     - Все категории, не входящие в список → **«Прочие услуги»**.  
     - Сентименты нормализованы через словарь (`позитивно` → `позитив`).  
     - Неизвестные тональности → **«нейтральный»**.

3. **Метрики**  
   - Рассчитывались **вне production-пайплайна** (в ноутбуках).  
   - Использовались только для презентации и финальной оценки:  
     - `F1-micro` по темам  
     - `Accuracy` по тональностям 

---

### 🔮 Планы по развитию (вне ТЗ)

**Глубокий сентимент-анализ внутри категорий**  
После основной классификации планируется автоматически выявлять **локальные тренды** внутри каждой категории (например, в «Картах»: *«скрытые комиссии»*, *«навязывание страховок»*, *«проблемы с UnionPay за границей»*).  

Для этого будет использоваться тот же подход, что и при формировании исходных категорий (LLM + кластеризация), но **внутри уже отфильтрованной выборки**. Цель — находить **топ-N повторяющихся проблем или позитивных аспектов** без необходимости вручную размечать всё.

> ⚠️ Фича находится в разработке и не входит в требования ТЗ.

---

Более подробные описания компонентов и логики см. в документации внутри соответствующих модулей.




  
